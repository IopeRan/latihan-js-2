        // Cara untuk mengetahui tipe data
        let data = 100; // bisa diganti string, boolean, null, dan undefined

        const typeData = typeof data;
        document.writeln(`<p>${typeData}</p>`) // number